# Checkout 计算规则

`calculate_total(items, shipping_fee, discount_percent)` 返回客户最终应付金额。

1. `items` 中每项是“商品单价、数量”二元组。
2. 百分比折扣只适用于商品小计，运费不参与折扣。
3. 商品折后金额加上运费后，按金融常规保留两位小数。
4. 折扣必须在 0 到 100 之间（含边界）；商品价格、数量和运费不能为负数。
5. 输入与输出使用 `Decimal`，避免浮点误差。
